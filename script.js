

function f1(command) {
    document.execCommand(command, false, null);
}

function f3(command) {
    document.execCommand(command, false, null);
}

function f2(command) {
    document.execCommand(command, false, null);
}
